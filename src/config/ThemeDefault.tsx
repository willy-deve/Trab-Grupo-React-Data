import { createTheme } from '@mui/material'

const ThemeDefault = createTheme({
  palette: {
    primary: {
      main: '#373B65',
    },
    secondary: {
      main: '#F89920',
    },
  },
})

export default ThemeDefault
